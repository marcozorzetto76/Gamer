{
  "name": "meteo-park",
  "version": "0.1.0",
  "private": true,
  "description": "Meteo Park - cozy merge game driven by real weather",
  "scripts": {
    "sync-www": "cp ../prototype/index.html www/index.html",
    "sync": "npm run sync-www && npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}